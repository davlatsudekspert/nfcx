<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.NFC"/>
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>

    <application
        <!-- TELEFONDA KO'RINADIGAN NOM — "NFCSTORE".
             "Nova" ichki loyiha nomi edi va u foydalanuvchiga hech
             narsa demaydi. Do'konda va telefon ekranida brend
             turishi kerak.
             `applicationId` (uz.nfcstore.nova) O'ZGARMAYDI: uni
             o'zgartirish yangi ilova yaratish va o'rnatilganlarni
             yo'qotish degani. -->
        android:label="NFCSTORE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- APP LINKS — FAQAT ILOVA EGALIK QILADIGAN YO'LLAR.
                 Ilgari bu yerda `host="nfcstore.uz"` turardi va YO'L
                 KO'RSATILMAGANDI. Bu butun domenni da'vo qilish
                 degani: saytning bosh sahifasi, do'kon, blog, admin
                 va to'lov sahifalari ham ilovaga tortilardi. Ilovada
                 esa ular uchun ekran YO'Q — odam sayt o'rniga
                 tushunarsiz joyga tushardi. `https://nfcstore.uz/`
                 esa `Routes.splash` ga (`/`) mos kelib, saytning
                 bosh sahifasi o'rniga ilova ochilardi.

                 Endi faqat haqiqatan boshqariladigan yo'llar:
                 profil, kompaniya vitrinasi, post, story va NFC.
                 Qolgan hamma narsa brauzerda ochiladi — to'g'ri
                 xulq, chunki sayt ularni o'zi ko'rsatadi.

                 JISMONIY KARTA TEGIZISH BUNDAN TA'SIRLANMAYDI: u
                 pastdagi `NDEF_DISCOVERED` filtri orqali keladi. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="nfcstore.uz" android:pathPrefix="/u/"/>
                <data android:scheme="https" android:host="nfcstore.uz" android:pathPrefix="/c/"/>
                <data android:scheme="https" android:host="nfcstore.uz" android:pathPrefix="/post/"/>
                <data android:scheme="https" android:host="nfcstore.uz" android:pathPrefix="/story/"/>
                <data android:scheme="https" android:host="nfcstore.uz" android:pathPrefix="/nfc/"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="https" android:host="nfcstore.uz"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
