name: Build NFCSTORE mobile release APK

on:
  workflow_dispatch: {}
  push:
    branches:
      - claude/dreamy-cerf-hkbsg0
    paths:
      - '.github/workflows/build-android-apk.yml'
      - 'nfcstore-mobile/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nfcstore-mobile
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install dependencies
        run: npm ci

      - name: Expo prebuild (generate native android/ project)
        run: npx expo prebuild --platform android --no-install

      - name: Build release APK (JS bundle embedded, debug-signed — no Metro needed at runtime)
        working-directory: nfcstore-mobile/android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Rename APK with run number
        run: |
          mkdir -p /tmp/apk-out
          cp android/app/build/outputs/apk/release/app-release.apk \
             /tmp/apk-out/nfcstore-mobile-release-${{ github.run_number }}.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: nfcstore-mobile-release-apk
          path: nfcstore-mobile/android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

      - name: Publish APK to a GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "apk-build-${{ github.run_number }}" \
            /tmp/apk-out/nfcstore-mobile-release-${{ github.run_number }}.apk \
            --repo "${{ github.repository }}" \
            --title "NFCSTORE mobile APK (build ${{ github.run_number }})" \
            --notes "Standalone build from commit ${{ github.sha }} — JS bundle embedded, no Metro server needed. NFC requires this build (Expo Go does not support it)." \
            --target "${{ github.sha }}"
