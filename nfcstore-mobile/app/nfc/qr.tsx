import { QrScreen } from '@/features/nfc/QrScreen';

/**
 * FAOL identity'ning QR + ulashish ekrani. Tab navigatordan tashqarida
 * (`p/[code]`, `c/[companyId]` bilan bir xil qatlamda).
 */
export default QrScreen;
