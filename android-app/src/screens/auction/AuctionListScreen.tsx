import React from 'react';
import { StubScreen } from '../shared/StubScreen';

/** Real 4-tab list (Live/Yaqinda/Tugagan/Men qatnashgan) lands in Phase 8. */
export function AuctionListScreen() {
  return <StubScreen screenName="Auksion" phase="Phase 8 — Auction" />;
}
