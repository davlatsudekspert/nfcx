import React from 'react';
import { StubScreen } from '../shared/StubScreen';

/** Real search-as-you-type against GET /api/records/search lands in Phase 7. */
export function IdSearchScreen() {
  return <StubScreen screenName="NFC ID qidirish" phase="Phase 7 — ID Purchase" />;
}
