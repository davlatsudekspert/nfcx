import React from 'react';
import { StubScreen } from '../shared/StubScreen';

/** Real "list mine / create CTA" wiring lands in Phase 10. */
export function CompanyHomeScreen() {
  return <StubScreen screenName="Kompaniya" phase="Phase 10 — Company" />;
}
