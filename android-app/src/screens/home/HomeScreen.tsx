import React from 'react';
import { StubScreen } from '../shared/StubScreen';

/** Real dashboard wiring (My IDs, Auctions, Company summary) lands in Phase 6. */
export function HomeScreen() {
  return <StubScreen screenName="Bosh sahifa" phase="Phase 6 — Home" />;
}
