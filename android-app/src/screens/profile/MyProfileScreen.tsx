import React from 'react';
import { StubScreen } from '../shared/StubScreen';

/** Real implementation (own NFC Profile View + edit affordances) lands in Phase 9. */
export function MyProfileScreen() {
  return <StubScreen screenName="Mening ID'larim" phase="Phase 9 — Personal Profile" />;
}
