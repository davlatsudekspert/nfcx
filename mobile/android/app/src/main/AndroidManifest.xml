<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET — RELEASE UCHUN SHART.
         Flutter shabloni bu ruxsatni faqat debug/profile manifestiga
         qo'yadi (u yerda hot reload uchun kerak). Release manifestida
         u YO'Q edi: ya'ni do'kondan o'rnatilgan ilova hech qanday
         so'rov yubora olmasdi va butunlay bo'sh ko'rinardi. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- NFC — kartani o'qish va bo'sh kartaga yozish uchun.
         `required="false"`: NFC'siz telefonlarda ham ilova
         o'rnatilsin, faqat NFC bo'limi "qo'llab-quvvatlanmaydi"
         deb ko'rsatadi. -->
    <uses-permission android:name="android.permission.NFC"/>
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>

    <!-- ZAXIRA NUSXA O'CHIRILGAN.
         Ilova xotirasida sessiya tokeni va ilova qulfining PIN'i
         turadi. Ular Android Keystore kaliti bilan shifrlangan,
         KALIT esa telefonni tark etmaydi. Ya'ni Google'ning
         avtomatik zaxirasi ma'lumotni bulutga ko'chirsa ham, uni
         boshqa telefonda OCHIB BO'LMAYDI — faqat foydalanuvchi
         "nega kirolmayapman" degan holatga tushardi.
         Shuning uchun zaxira umuman olinmaydi: xavfsizroq ham,
         tushunarliroq ham (yangi telefonda oddiy qayta kirish). -->
    <!-- "NFCSTORE Classic" — yangi ilova endi shunchaki
         "NFCSTORE" deb ataladi. Ikkalasi bitta telefonda turishi
         mumkin, shuning uchun nomlari farq qilishi SHART; aks
         holda odam qaysi birini ochayotganini bilmaydi.
         `applicationId` (uz.nfcstore.app) O'ZGARMAYDI —
         o'rnatilgan ilovalar yangilanishda davom etadi. -->
    <application
        android:label="NFCSTORE Classic"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- APP LINKS — jismoniy NFC kartani tegizganda brauzer
                 emas, SHU ILOVA ochilsin.

                 `autoVerify` ishlashi uchun serverda
                 https://nfcstore.uz/.well-known/assetlinks.json
                 turishi va unda ilovani imzolagan sertifikatning
                 SHA-256 barmoq izi bo'lishi SHART. Worker uni
                 `ANDROID_APP_FINGERPRINTS` env'idan beradi.
                 Tasdiqlanmasa ilova ochilmaydi, lekin hech narsa
                 buzilmaydi — havola avvalgidek brauzerda ochiladi. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="nfcstore.uz"/>
                <data android:scheme="https" android:host="www.nfcstore.uz"/>
            </intent-filter>

            <!-- NFC tegini tizim o'zi o'qiganda (ilova yopiq bo'lsa ham)
                 to'g'ridan-to'g'ri shu ilovaga berilsin. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="https" android:host="nfcstore.uz"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- ANDROID 11+ PAKET KO'RINISHI — quyidagilarsiz telefon,
             Telegram, WhatsApp va brauzerni ochish JIMGINA ishlamaydi:
             tizim mos ilovani "ko'rmaydi" va `launchUrl` false
             qaytaradi. Kontakt amallari va Payme/Click to'lov
             sahifasi shunga bog'liq. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
